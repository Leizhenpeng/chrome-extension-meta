{
  "name": "chrome-extension-meta",
  "version": "0.0.1",
  "description": "A Node.js package to fetch statistics from the Chrome Web Store",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "mocha",
    "test-watch": "mocha --reporter spec --watch",
    "start": "node index.js",
    "coverage": "nyc npm test && nyc report --reporter=text-lcov | coveralls",
    "change-log": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/leizhenpeng/chrome-extension-meta"
  },
  "keywords": [
    "chrome",
    "extension",
    "webstore",
    "stats"
  ],
  "author": "river laolei@forkway.cn",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.0"
  },
  "homepage": "https://github.com/leizhenpeng/chrome-extension-meta",
  "devDependencies": {
    "chai": "^4.2.0",
    "conventional-changelog-cli": "^4.1.0",
    "coveralls": "^3.1.0",
    "eslint": "^7.5.0",
    "mocha": "^8.0.1",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^15.1.0"
  }
}